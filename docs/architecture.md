# Architecture

Используем Feature-Sliced Design.
Можем добавлять собственные слои, если на то есть причина.

Из сторонних слайсов можно импортировать только то,
что явно ре-экспортируется ими в `index.ts`.
